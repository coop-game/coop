/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // experimental: {
  //   transpilePackages: ["@my/jjdraw"],
  // },
};

module.exports = nextConfig;
