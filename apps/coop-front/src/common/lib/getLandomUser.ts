import lodashRandom from "lodash/random";
import { CPUserProfile } from "@types";

const getLandomUser = (userProfiles: CPUserProfile) => {
  lodashRandom();
  return 123;
};
export default getLandomUser;
