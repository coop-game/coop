import { CPGames } from "@types";

export const DEFAULT_GAME_STATE: CPGames = {
  DRAWEE: {
    isGameStart: true,
    path: "/start",
  },
  RELAY_RACE: {
    isGameStart: true,
    path: "/start",
  },
};
